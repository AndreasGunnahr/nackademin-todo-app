const { ROLE } = require("../utils/roles");

const canViewTodo = (user, todo) => {};

const canCreateTodo = (user, todo) => {};

const canEditTodo = (user, todo) => {};

const canDeleteTodo = (user, todo) => {};

module.exports = {};
